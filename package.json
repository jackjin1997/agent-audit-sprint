{
  "name": "agent-audit-sprint",
  "version": "1.0.0",
  "private": true,
  "description": "Static landing page and sales assets for a $1,000 fixed-price Agent/MCP audit service.",
  "type": "module",
  "bin": {
    "agent-mcp-audit": "tools/agent-mcp-audit.mjs"
  },
  "scripts": {
    "audit:repo": "node tools/agent-mcp-audit.mjs",
    "lint": "node --check tools/agent-mcp-audit.mjs && node --check scripts/check-site.mjs && node --check scripts/render-assets.mjs && node --check scripts/render-payment-qr.mjs && node --check script.js",
    "test": "npm run check",
    "typecheck": "npm run lint",
    "render:assets": "node scripts/render-assets.mjs && node scripts/render-payment-qr.mjs",
    "render:qr": "node scripts/render-payment-qr.mjs",
    "check": "node scripts/check-site.mjs"
  },
  "keywords": [
    "mcp",
    "agents",
    "audit",
    "security"
  ],
  "author": "Zexu Jin",
  "license": "MIT",
  "devDependencies": {
    "playwright": "^1.60.0",
    "qrcode": "^1.5.4"
  }
}
